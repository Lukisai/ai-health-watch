export default function ReportForm() {
    return (
      <div>
        <h1 className="text-xl font-semibold">Report a risky chatbot interaction (no PII)</h1>
        <p className="text-sm text-slate-600 mt-2">Form coming next — routing smoke test.</p>
      </div>
    );
  }